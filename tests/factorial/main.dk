import .test_import

def factorial(n):
    if n <= 1:
        return 1
    else:
        return n * factorial(n - 1)

a = factorial(5)
b = factorial(3)
c = factorial(2)
combos = a / (b * c)

print("Combinations N=5, K=3:", pformat(combos))
